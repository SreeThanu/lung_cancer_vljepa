# Lab machine requirements: Ubuntu 22.04, RTX A4000, CUDA 12.x
# Install with:
#   pip install -r requirements-lab.txt
#
# PyTorch MUST be installed from the CUDA wheel index BEFORE this file:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Verify GPU is visible after install:
#   python -c "import torch; print(torch.cuda.get_device_name(0))"

# ── Core ML / DL ──────────────────────────────────────────────────────────────
# torch, torchvision, torchaudio installed separately from CUDA wheel index (see above)
monai==1.3.2              # DenseNet121 3D + medical imaging utilities
numpy==1.26.4
scipy==1.13.1

# ── Data / preprocessing ──────────────────────────────────────────────────────
pandas==2.2.2
scikit-learn==1.5.1
pydicom==2.4.4            # DICOM reading (used by verify_lab_setup.py)
SimpleITK==2.3.1          # DICOM resampling
pylidc==0.2.3             # LIDC annotation parsing
lxml==5.2.2               # XML parsing for LIDC XMLs
tqdm==4.66.4

# ── Radiomics (Phase 8 — installed but use_radiomics: false by default) ───────
#pyradiomics==3.0.1

# ── Notebooks / visualization ─────────────────────────────────────────────────
jupyterlab==4.2.4
matplotlib==3.9.1
seaborn==0.13.2
ipywidgets==8.1.3

# ── Config / utilities ────────────────────────────────────────────────────────
pyyaml==6.0.1
psutil==6.0.0
